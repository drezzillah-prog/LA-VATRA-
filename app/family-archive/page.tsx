import Link from 'next/link';import ProductCard from '@/components/ProductCard';import {products} from '@/data/catalog';import {getRequestContext} from '@/lib/localization';
const questions=['Are your grandparents still able to tell their stories?','Do you have photographs nobody can fully identify?','Has your family moved between villages, cities or countries?','Are there recipes nobody has formally written down?','Are there children or grandchildren you want to leave this history to?'];
export const metadata={title:'Arhiva Familiei'};
export default async function FamilyArchive(){const {region}=await getRequestContext();const list=products.filter(p=>p.tags.includes('family-memory')).slice(0,12);return <><section className="special-hero family-hero"><div className="shell"><p className="eyebrow">Arhiva Familiei</p><h1>Keep it while someone still remembers.</h1><p>Names. Voices. Homes. Journeys. Photographs. Recipes. Begin with the memory most likely to disappear first.</p></div></section><section className="section archive-builder"><div className="shell archive-builder-grid"><div><p className="eyebrow">Build Your Family Archive</p><h2>Every family has a different archive.</h2><p>Answer these questions for yourself, then start with the objects that match what your family still carries.</p></div><ol>{questions.map(q=><li key={q}>{q}</li>)}</ol></div></section><section className="section"><div className="shell"><div className="section-heading split"><div><p className="eyebrow">Start here</p><h2>Objects for family memory.</h2></div><Link className="text-link" href="/rooms/casa-mare">Enter Casa Mare <span>→</span></Link></div><div className="product-grid">{list.map(p=><ProductCard key={p.slug} product={p} region={region}/>)}</div></div></section></>;}
