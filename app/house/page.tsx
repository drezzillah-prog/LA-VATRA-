import Link from 'next/link';import {rooms} from '@/data/catalog';import Newsletter from '@/components/Newsletter';
export const metadata={title:'Casa Vetrei'};
export default function HousePage(){return <><section className="threshold-hero"><div className="shell"><p className="eyebrow">Casa Vetrei</p><h1>Intră în casă.</h1><p>The homepage is the threshold. The rooms are where the objects live.</p></div></section><section className="room-threshold-grid">{rooms.map(r=><Link href={'/rooms/'+r.slug} key={r.key} className={'threshold-room threshold-room--'+r.key} style={{backgroundImage:`url("${r.photo}")`}}><span/><div><small>{r.modern}</small><h2>{r.title}</h2><p>{r.kicker}</p><b>Intră în odaie →</b></div></Link>)}</section><Newsletter/></>;}
