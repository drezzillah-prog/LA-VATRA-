import ProductCard from '@/components/ProductCard';import {products} from '@/data/catalog';import {getRequestContext} from '@/lib/localization';
const groups=[['For Mum','The stories she thinks everybody already knows.'],['For Dad','Places, objects, photographs and family history.'],['For Grandparents','Questions worth asking while they can still answer them.'],['For Someone Abroad','A place for everything that still feels like home.'],['For a Wedding','Not just two people. Two family histories meeting.'],['For Christmas','Something the family can still be talking about next Christmas.'],['For a New Home','A way to connect the new house to the ones that came before.']];
export const metadata={title:'Gifts'};
export default async function Gifts(){const {region}=await getRequestContext();const giftProducts=products.filter(p=>p.tags.includes('gift')).slice(0,12);return <><section className="special-hero gifts-hero"><div className="shell"><p className="eyebrow">Cadouri</p><h1>Give them something nobody else could have filled in for them.</h1></div></section><section className="section"><div className="shell gift-reasons">{groups.map(([a,b])=><article key={a}><h2>{a}</h2><p>{b}</p></article>)}</div></section><section className="section"><div className="shell"><div className="section-heading"><p className="eyebrow">Giftable objects</p><h2>Chosen for memory, not clutter.</h2></div><div className="product-grid">{giftProducts.map(p=><ProductCard key={p.slug} product={p} region={region}/>)}</div></div></section></>;}
