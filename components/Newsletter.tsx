'use client';import {FormEvent,useState} from 'react';
export default function Newsletter(){const [message,setMessage]=useState('');function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setMessage('You are at the threshold. Email delivery will open with the shop connection.');}
return <section className="newsletter" id="newsletter"><div className="shell newsletter-inner"><div><p className="eyebrow">Letters from the Vatra</p><h2>Things worth keeping.</h2><p>New editions, seasonal pages, family-archive prompts and occasional letters from the house.</p></div><form onSubmit={submit} className="newsletter-form"><label htmlFor="newsletter-email">Email address</label><div><input required id="newsletter-email" type="email" placeholder="you@example.com"/><button type="submit">Keep me close</button></div>{message&&<p className="form-note" role="status">{message}</p>}</form></div></section>;}
